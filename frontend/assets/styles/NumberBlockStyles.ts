import { StyleSheet } from 'react-native'

export const NumberBlockStyles = StyleSheet.create({
    container: {
        width: 100,
        height: 100,
        borderRadius: 20,
        justifyContent: 'center',
        alignItems: 'center',
    },
    text: {
        fontSize: 72,
    },
})